# Shared Notes

Use this folder for definitions shared across tesyt4.

Suggested folder use:

- `types`: shared data shapes and interface notes.
- `constants`: shared names, limits, and feature constants.

Avoid placing secrets, credentials, or environment-specific values here.
